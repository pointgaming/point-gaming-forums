<h1><%= link_to t('forem.forum.forums'), forem.forums_path %> &raquo; <%= link_to forem_emojify(topic.forum.title), [forem, topic.forum] %> &raquo; <%= forem_emojify(topic.subject) %></h1>
<% if forem_admin? %>
  <div class="breadcrumb">
    <%= link_to t('forem.topic.links.edit'), forem.edit_admin_topic_path(topic), :class => 'btn' %>
  </div>
<% end %>
